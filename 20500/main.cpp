#include <iostream>

using namespace std;

const int DIV = 1000000007;

// 마지막 자리가 1인 경우 절대 15의 배수가 될 수 없음. 
// 모든 자리의 수를 더한값이 3으로 나누어떨어지면 0이됨

int main (void) {
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	
	ll dp[1516][3];
	int n;
	cin >> n;


		

	for ( int i = 1 ; i <= n ; i++ ) {
		for ( int j = 1; j <= i; j++ ) {
			
				

	return 0;
}
